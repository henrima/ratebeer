<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>

<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery_id %>
</p>

<p>
	<% if @beer.ratings.empty? %>
 		beer has not yet been rated!
	<% else %>
 		<p> Has <%= pluralize(@beer.ratings.count, 'rating') %>, average <%= @beer.avarage_rating %></p>
	<% end %>
</p>

<%= link_to 'Edit', edit_beer_path(@beer) %> |

